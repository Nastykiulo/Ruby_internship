<%= form_with(model: answer_item, local: true) do |form| %>
  <% if answer_item.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(answer_item.errors.count, "error") %> prohibited this answer_item from being saved:</h2>

      <ul>
      <% answer_item.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :title %>
    <%= form.text_field :title %>
  </div>

  <div class="field">
    <%= form.label :correct %>
    <%= form.check_box :correct %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
