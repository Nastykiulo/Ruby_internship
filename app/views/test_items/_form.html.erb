<%= form_with(model: test_item, local: true) do |form| %>
  <% if test_item.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(test_item.errors.count, "error") %> prohibited this test_item from being saved:</h2>

      <ul>
      <% test_item.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :assignment_id %>
    <%= form.text_field :assignment_id %>
  </div>

  <div class="field">
    <%= form.label :pass %>
    <%= form.check_box :pass %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
