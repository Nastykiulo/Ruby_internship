<%= form_with(model: assignment, local: true) do |form| %>
  <% if assignment.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(assignment.errors.count, "error") %> prohibited this assignment from being saved:</h2>

      <ul>
      <% assignment.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :student_id %>
    <%= form.select :student_id, options_for_select(Student.all.map{|s|[s.id]}) %>
    <%#= form.text_field :student_id %>
  </div>

  <div class="field">
    <%= form.label :test_id %>
    <%= form.select :test_id, options_for_select(Test.all.map{|t|[t.title, t.id]}) %>
    <%#= form.select :test_id, options_for_select(@test.map{ |t| [t.id, t.title] }) %>
    <%#= form.text_field :test_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
