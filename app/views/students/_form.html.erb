<%= form_with(model: [@teacher, student], url: student_session_path(student), local: true) do |form| %>
  <% if student.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(student.errors.count, "error") %> prohibited this student from being saved:</h2>

      <ul>
      <% student.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :id %>
    <%= form.text_field :id %>
  </div>

  <div class="field">
    <%= form.label :email %>
    <%= form.text_field :email %>
  </div>

  <div class="field">
    <%= form.number_field :user_id, id: :student_user_id, value: current_teacher.id, type: :hidden %>
  </div>

  <div class="actions">
    <%= form.submit, class:"btn btn-light"  %>
  </div>
<% end %>
